---
name: graphite-ds
description: 石墨极简设计系统的 token 正本与 agent 适配层。做页面、文档、图表、示意图时统一视觉身份。用户提到石墨、灰阶、极简、我们的设计系统、按我们的风格，或需要在同一份产物里同时出现正文排版与图表时使用。不负责公众号排版本体（走 gzh-design-skill）、图表渲染（走 lieflat-chart）、示意图绘制（走 diagram-design）。
---

# Graphite DS

一套 token，三个适配层。本 skill 不渲染任何东西，只负责让三个渲染 skill 用同一套视觉身份。

## 先读这个

token 正本是 `tokens/graphite.json`，其余一切从它派生。**不要在别处硬写色值。**

对比度实测与橙色为什么有两个值，见 `tokens/contrast.md`。

## 按任务分流

| 要做的东西 | 去哪 |
|---|---|
| 公众号文章、长文排版 | `gzh-design-skill`，主题选石墨极简；深色补丁见 `adapters/gzh/` |
| 数据图表（有量、有维度） | `lieflat-chart`，锁 `graphite` 预设；先跑 `adapters/lieflat/apply.sh` |
| 示意图（分层、流程、拓扑、组件关系） | `diagram-design`，皮肤用 `adapters/diagram-design/style-guide.md` |
| 网页、方案文档 | 直接用 `tokens/graphite.css`，结构参照石墨极简的组件形态 |

## 图表还是示意图

分界是一个问题：**每个视觉通道对应哪个数据维度？**

答得出就是图表，走 lieflat。答不出就是示意图，走 diagram-design。分层、流程、组件关系没有量，硬做成图表会变成抄形不抄魂。

两个都别用的情况也存在：表格能说清就用表格。

## 红线

1. **同一份产物只能有一套色系。** 正文、图表、示意图必须同源，否则三套灰的色温会打架。
2. **橙色分场景取值。** 排版 `#F97316`，图表与图形 `#EA580C`，深色底 `#FB923C`。不要混用。
3. **焦点每图最多一个。** 第二个焦点等于没有焦点。
4. **中文最小 12px。** 上游那些 7–9px 的等宽小标签槽位遇到中文一律升到 12px sans，去掉字距和大写。
5. **不加载网络字体。** 系统栈，CJK 交给系统解析。
6. **不引入第二色相。** 需要区分时用明度、线型或位置，不要加蓝加绿。

## 边界

三个上游许可证互不兼容，本仓库只发增量不收代码。改动都作用在本地安装上，上游升级会覆盖，需重跑适配脚本。详见 `NOTICE.md`。
